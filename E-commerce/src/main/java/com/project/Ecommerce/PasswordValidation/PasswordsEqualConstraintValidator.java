package com.project.Ecommerce.PasswordValidation;/*
package com.springbootcamp.springsecurity.PasswordValidation;

import com.springbootcamp.springsecurity.DTO.CustomerDto;
import com.springbootcamp.springsecurity.Entities.Customer;
import com.springbootcamp.springsecurity.Entities.User;

import javax.validation.ConstraintValidator;
import javax.validation.ConstraintValidatorContext;

public class PasswordsEqualConstraintValidator implements ConstraintValidator<PasswordEqualCheck , Object> {
    @Override
    public void initialize(PasswordEqualCheck constraintAnnotation) {

    }

    @Override
    public boolean isValid(Object value, ConstraintValidatorContext context) {
        CustomerDto user = (CustomerDto) value;
        return user.getPassword().equals(user.getConfirmPassword());
    }
}
*/
